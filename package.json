{
  "name": "ClashFox",
  "version": "0.3.8",
  "buildNumber": 7,
  "description": "ClashFox GUI for Mihomo",
  "author": "Kuochiang Lu <lukuochiang@gmail.com> (https://github.com/lukuochiang/ClashFox)",
  "main": "src/main.js",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "dev": "node scripts/bump-version.js && CLASHFOX_DEV=1 electron .",
    "build:mac": "node scripts/clean-dist-mac.js --pre; CLASHFOX_BUILD_NUMBER=$(node -p \"require('./package.json').buildNumber\") CSC_IDENTITY_AUTO_DISCOVERY=false PYTHON=${PYTHON:-python3} sh -c 'electron-builder --mac zip --x64 && electron-builder --mac zip --arm64 && electron-builder --mac zip --universal'; code=$?; node scripts/clean-dist-mac.js; exit $code",
    "clean:mac": "node scripts/clean-dist-mac.js"
  },
  "devDependencies": {
    "electron": "^28.2.0",
    "electron-builder": "^26.8.1",
    "electron-reload": "^1.5.0"
  },
  "build": {
    "appId": "com.clashfox.gui",
    "productName": "ClashFox",
    "files": [
      "src/**/*",
      "scripts/**/*",
      "assets/**/*",
      "static/**/*",
      "package.json",
      "!**/dist/**",
      "!**/node_modules/**",
      "!**/.DS_Store"
    ],
    "asarUnpack": [
      "scripts/**"
    ],
    "mac": {
      "category": "public.app-category.utilities",
      "icon": "src/ui/assets/icns/logo.icns",
      "identity": null,
      "artifactName": "${productName}-${version}(${env.CLASHFOX_BUILD_NUMBER})-mac-${arch}.${ext}",
      "target": [
        {
          "target": "zip",
          "arch": [
            "x64",
            "arm64",
            "universal"
          ]
        }
      ]
    },
    "dmg": {
      "window": {
        "width": 540,
        "height": 380
      },
      "contents": [
        {
          "x": 160,
          "y": 160
        },
        {
          "x": 380,
          "y": 160,
          "type": "link",
          "path": "/Applications"
        }
      ],
      "title": "ClashFox Installer"
    }
  }
}
